name: emoji_count
description: A count app for emojis!

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.4
  google_fonts: ^3.0.1
  collection: ^1.16.0
  shared_preferences: ^2.0.15
  emoji_picker_flutter: ^1.5.2
  bordered_text: ^2.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
